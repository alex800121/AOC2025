module Day18 where

import Paths_AOC2025 (getDataDir)

day18 :: IO (String, String)
day18 = do
  -- input <- (readFile . (++ "/input/input18.txt") =<< getDataDir)
  let !finalAnsa =
        show $
          ()
  let !finalAnsb =
        show $
          ()
  pure (finalAnsa, finalAnsb)
