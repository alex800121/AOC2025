module Day8 where

import Paths_AOC2025 (getDataDir)

day8 :: IO (String, String)
day8 = do
  -- input <- (readFile . (++ "/input/input8.txt") =<< getDataDir)
  let !finalAnsa =
        show $
          ()
  let !finalAnsb =
        show $
          ()
  pure (finalAnsa, finalAnsb)
