module Day15 where

import Paths_AOC2025 (getDataDir)

day15 :: IO (String, String)
day15 = do
  -- input <- (readFile . (++ "/input/input15.txt") =<< getDataDir)
  let !finalAnsa =
        show $
          ()
  let !finalAnsb =
        show $
          ()
  pure (finalAnsa, finalAnsb)
