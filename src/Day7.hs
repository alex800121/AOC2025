module Day7 where

import Paths_AOC2025 (getDataDir)

day7 :: IO (String, String)
day7 = do
  -- input <- (readFile . (++ "/input/input7.txt") =<< getDataDir)
  let !finalAnsa =
        show $
          ()
  let !finalAnsb =
        show $
          ()
  pure (finalAnsa, finalAnsb)
